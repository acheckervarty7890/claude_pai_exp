# Secrets — never commit
.env
close_this.sh

# Python virtualenv & caches
.venv/
__pycache__/
*.pyc

# Local editor/agent settings
.claude/settings.local.json

# Run logs (reproducible noise)
*.log

# Local baseline snapshots (baseline state is preserved in git history on main)
*.baseline.pkl
*.baseline.csv
